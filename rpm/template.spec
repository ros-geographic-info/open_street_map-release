Name:           ros-indigo-test-osm
Version:        0.2.3
Release:        0%{?dist}
Summary:        ROS test_osm package

Group:          Development/Libraries
License:        BSD
URL:            http://ros.org/wiki/test_osm
Source0:        %{name}-%{version}.tar.gz

Requires:       ros-indigo-geodesy
Requires:       ros-indigo-geographic-msgs
Requires:       ros-indigo-osm-cartography
Requires:       ros-indigo-route-network
BuildRequires:  ros-indigo-catkin
BuildRequires:  ros-indigo-geodesy
BuildRequires:  ros-indigo-geographic-msgs
BuildRequires:  ros-indigo-osm-cartography
BuildRequires:  ros-indigo-rostest
BuildRequires:  ros-indigo-route-network

%description
These are regression tests for the osm_cartography and route_network packages.
They are packaged separately to avoid unnecessary implementation dependencies.

%prep
%setup -q

%build
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree that was dropped by catkin, and source it.  It will
# set things like CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/indigo/setup.sh" ]; then . "/opt/ros/indigo/setup.sh"; fi
mkdir -p obj-%{_target_platform} && cd obj-%{_target_platform}
%cmake .. \
        -UINCLUDE_INSTALL_DIR \
        -ULIB_INSTALL_DIR \
        -USYSCONF_INSTALL_DIR \
        -USHARE_INSTALL_PREFIX \
        -ULIB_SUFFIX \
        -DCMAKE_INSTALL_PREFIX="/opt/ros/indigo" \
        -DCMAKE_PREFIX_PATH="/opt/ros/indigo" \
        -DSETUPTOOLS_DEB_LAYOUT=OFF \
        -DCATKIN_BUILD_BINARY_PACKAGE="1" \

make %{?_smp_mflags}

%install
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree that was dropped by catkin, and source it.  It will
# set things like CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/indigo/setup.sh" ]; then . "/opt/ros/indigo/setup.sh"; fi
cd obj-%{_target_platform}
make %{?_smp_mflags} install DESTDIR=%{buildroot}

%files
/opt/ros/indigo

%changelog
* Mon Oct 12 2015 Jack O'Quin <jack.oquin@gmail.com> - 0.2.3-0
- Autogenerated by Bloom

* Thu Oct 08 2015 Jack O'Quin <jack.oquin@gmail.com> - 0.2.2-0
- Autogenerated by Bloom

